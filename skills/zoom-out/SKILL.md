---
name: zoom-out
description: 当不熟悉某段代码、模块关系不清、需要理解系统全局、调用链、数据流、领域语言或改动影响范围时使用。
---

# Zoom Out

## 全局理解检查点

当你不理解某段代码在系统中的位置、调用者、依赖、数据流或领域含义时，先使用 `zoom-out`。

不要在只理解局部文件的情况下直接修改跨模块行为。

## 目标

把局部代码放回系统地图里理解。

输出应帮助后续 `clarify`、`grill`、`plan`、`tdd` 或 `diagnose` 做出更好的判断。

## 流程

1. 找到当前关注的文件、函数、模块或行为。
2. 向上追溯调用者和入口。
3. 向下查看它依赖的模块、数据结构、外部服务或副作用。
4. 查找相关测试、文档、`CONTEXT.md`、ADR 或 README。
5. 用项目已有领域语言描述它，不要发明不必要的新词。
6. 标出事实、推断和未确认点，不要混在一起。
7. 如果发现结构性摩擦，再转入 `deepen`，不要在 `zoom-out` 中直接提出重构方案。

## 输出结构

必须输出：

- 系统位置：它属于哪个模块/上下文，负责什么。
- 调用关系：谁调用它，它又调用谁。
- 数据流：关键输入、输出、状态变化和副作用。
- 领域语言：相关术语及其含义。
- 改动影响：修改这里可能影响哪些行为或测试。
- 事实 / 推断 / 未确认：分别列出证据充分的事实、基于证据的推断、仍需确认的问题。
- 不确定点：哪些地方还需要代码、文档或用户确认。

## 转入 deepen

出现以下情况时，结束全局理解并转入 `deepen`：

- 同一领域概念散落在多个模块，理解需要反复跳转。
- 修改一个行为会牵动多个无关文件。
- 当前接口难以写行为测试。
- 模块很浅，调用者仍要理解大量内部细节。
- 已发现隐藏耦合、测试入口缺失或架构方向错误。

## 不要做

- 不要直接写实现。
- 不要把猜测当事实。
- 不要只复述文件内容。
- 不要输出过大的全仓库地图，只覆盖和当前任务相关的范围。
